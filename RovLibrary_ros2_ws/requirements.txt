# Windows reproducible dependency set.
#
# pymavlink 2.4.49 is available as cp38 and cp312 Windows wheels.
# On Python 3.8, pip may try fastcrc 0.3.6 from source and fail while
# installing build dependencies, so use the known cp38 wheel-compatible pin.
# ROS 2 Lyrical Windows binaries are built for Python 3.12 and can use 0.3.6.
fastcrc==0.3.2; python_version < "3.9"
fastcrc==0.3.6; python_version >= "3.9"
pymavlink==2.4.49
pytest>=7.0,<8.0
pytest-rerunfailures>=12.0,<14.0

# Python-side dependencies needed by the Windows ROS 2 zip when it is patched
# to use an external conda Python interpreter.
lark>=1.1
typing_extensions>=4.8
osrf-pycommon>=2.0
PyYAML>=6.0
psutil>=5.9
defusedxml>=0.7
pydot>=1.4
numpy>=1.24,<2.0; python_version < "3.9"
numpy>=2.0; python_version >= "3.9"
